import type { Breakpoint } from '@mui/material/styles';

interface FooterProps {
  layoutQuery?: Breakpoint;
}

export function Footer({ layoutQuery }: FooterProps) {
  // Simple footer - can be enhanced later
  return null;
}
